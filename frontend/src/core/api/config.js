// Centralized API host and base path for the app
// Update `API_HOST` when testing on device or emulator.
const API_HOST = 'http://localhost:5000';
const API_BASE = `${API_HOST}/api`;

export { API_HOST, API_BASE };
